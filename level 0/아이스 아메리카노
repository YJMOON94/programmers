function solution(money) {
    let answer = [];
    
    let changes = 0;
    let coffee = 0;
    
    coffee = Math.floor(money/5500);
    
    changes = money - (coffee*5500);
    
    answer[0] = coffee;
    answer[1] = changes;

    
    return answer;
}
